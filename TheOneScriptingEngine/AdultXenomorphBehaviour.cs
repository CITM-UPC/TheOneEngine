using System;

public class AdultXenomorphBehaviour : MonoBehaviour
{
	//IGameObject playerGO;
	//Vector3 directorVector;

	//bool chasing = false;
	//float movementSpeed = 15.0f;

	public override void Start()
	{
		//playerGO = IGameObject.Find("Player");
	}

	public override void Update()
	{
		//directorVector = (playerGO.transform.position - attachedGameObject.transform.position).Normalize();

		//if (!chasing && Vector3.Distance(playerGO.transform.position, attachedGameObject.transform.position) < 10.0f)
  //      {
		//	chasing = true;
  //      }

		//if (!chasing) return;

		//attachedGameObject.transform.rotation = directorVector;
		//attachedGameObject.transform.Translate(attachedGameObject.transform.forward * movementSpeed * Time.deltaTime);
	}
}